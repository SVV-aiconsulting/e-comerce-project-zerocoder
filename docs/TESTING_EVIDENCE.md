# Тестирование и доказательства готовности MVP

Версия: 1.1 (развёрнутый релиз модернизации)
Дата актуализации: 09.09.2026

## 1. Автоматические проверки актуального релиза

| Набор | Результат |
|---|---:|
| Backend/Django, сервисы, интеграционные и contract-тесты | 288 passed |
| Telegram adapter | 42 passed |
| VK adapter | 31 passed |
| Всего | **361 passed** |
| `makemigrations --check --dry-run` / `manage.py check` | успешно |
| `compileall` | успешно |
| Compose development и production config | успешно |
| `git diff --check` | успешно |

Покрыты согласие и отзыв ПДн, каталог и синонимы, общая корзина ручного/AI
checkout, монотонная версия корзины и неизменяемый preview, единый контроль доступа,
одноразовый вход по email-коду, типизированные tool calls, восстановление очереди,
единственное подтверждение, идемпотентные заказ, платёж и возврат, доставка,
webhook/polling ЮKassa, обезличивание клиента, email и доступ к dashboard.

Последний полный прогон выполнен в GitHub Actions перед сборкой immutable-образов.
Тесты используют изолированную БД и не изменяют рабочие данные PostgreSQL.

### Проверки кандидата модернизации

- Все 69 миграций применены с нуля к отдельной PostgreSQL 16.
- Backup этой БД восстановлен в другую отдельную БД; подтверждены 69 миграций и
  49 прикладных таблиц. Обе тестовые БД не содержали production-данных.
- Live evaluation прежнего structured extraction GigaChat: 6/6 (100%).
- Live smoke нового консультанта: «Покажи лосось и форель…» → «Какой из них
  дешевле?» сохраняет ссылочный контекст, повторно читает обе карточки backend и
  сообщает актуальную минимальную цену за одинаковую единицу измерения.
- Страница кабинета проверена в локальном и публичном браузере: скрытые состояния форм работают,
  доступные заказы ограничены сессией, ошибок и предупреждений консоли нет.
- Страница возврата ЮKassa показывает только доступный текущей сессии заказ,
  фактический статус и ручную повторную проверку; пустое состояние и консоль проверены.

Полный записанный live-прогон 40 сценариев из `AI_DIALOGUE_ACCEPTANCE.md` остаётся
отдельным артефактом формальной приёмки диалога. Критичные серверные сценарии из
этой матрицы покрыты автоматическими тестами, а публичный live smoke сравнения и
реальные sandbox-интеграции пройдены. Старый способ ответа доступен оперативно через
`AI_CONSULTANT_ENABLED=False` без отключения исправлений доступа и checkout.

## 2. CI/CD и VPS

На VPS развёрнут релиз модернизации. GitHub, checkout VPS и образы контейнеров
используют один и тот же immutable SHA.

Pipeline `.github/workflows/deploy.yml` выполняет backend-, Telegram- и VK-тесты,
собирает immutable SHA-образы в GitHub Actions, публикует их в публичном GHCR и
разворачивает на VPS без сборки на сервере.

Для релиза `3eb66066aafaf0ab140a3bf584f4ff67f287e8fe` подтверждены:

- GitHub Actions run `34318426083` завершён успешно: backend, Telegram, VK, сборка,
  публикация образов и deploy;
- все новые миграции `common`, `customers`, `intake`, `payments` и `delivery` применены;
- `web`, `nginx`, PostgreSQL, Redis, Celery beat, три worker-очереди, Telegram и VK
  запущены; обязательные healthchecks успешны;
- heartbeat в БД актуален для `bot:telegram`, `bot:vk`, `worker:intake`,
  `worker:payments` и `worker:delivery`;
- `/`, `/account/`, `/payment/return/`, `/api/health/` и `/api/ready/` отвечают 200
  по HTTPS; HTTP перенаправляется на HTTPS;
- перед обновлением создан backup production-БД; ранее подтверждено восстановление
  backup в отдельную PostgreSQL;
- ручной guest checkout создаёт заказ, повтор подтверждения возвращает тот же заказ,
  изменение количества аннулирует preview с `409 preview_stale`, а чужая браузерная
  сессия получает 404 на детали;
- ЮKassa sandbox создала платёжную ссылку для сохранённого тестового заказа;
- Яндекс Доставка test рассчитала реальный quote: 363,56 ₽, срок 2 дня;
- новый GigaChat-диалог на публичном стенде сохранил контекст сравнения и на вопрос
  «Какой из них дешевле?» вернул серверные цены: форель 1450 ₽, лосось 1800 ₽;
- Telegram после одного стартового сетевого timeout автоматически восстановил polling;
  VK Long Poll запущен штатно;
- `/manager/dashboard/` защищён авторизацией Django Admin.

Публичный `AI_CONSULTANT_ENABLED=True`. Для быстрого эксплуатационного отката подачи
ответов достаточно вернуть флаг в `False` и перезапустить `web` и intake worker;
серверные исправления F1–F6 при этом остаются включёнными.

Предупреждения `manage.py check --deploy` о Django HSTS/redirect учитываются вместе
с фактическим TLS termination и HTTP→HTTPS redirect в nginx. До обработки реальных
коммерческих данных необходимо вернуться к checklist `docs/SECRET_ROTATION.md`.
Фактическая ротация исключена из текущего MVP решением владельца и не выполняется
на работающем демонстрационном стенде.

## 3. Контрольный сценарий защиты

Использовать только тестовые контакты и адрес. Перед записью очистить демонстрационную
корзину и убедиться, что тестовый магазин ЮKassa и test-контур Яндекс включены.

### Часть A — website и согласие

1. Открыть сайт в приватном окне и показать каталог.
2. Открыть AI-консультанта: composer скрыт, показаны ссылки на Политику/Согласие и
   отдельные действия «Согласен»/«Не согласен».
3. Дать согласие; запросить «Какая есть красная рыба?» и показать только фактическую
   выборку каталога с ценой и минимумом.
4. Добавить товар через AI, затем изменить количество вручную в той же корзине и
   попросить ассистента показать состав — позиции должны совпасть.
5. Продолжить checkout: доставка → адрес → имя/телефон → карта онлайн → email чека.
6. Показать server-side preview с тарифом Яндекс Доставки и один раз подтвердить.
7. Получить номер CRM-заказа и одну ссылку тестовой ЮKassa.

### Часть B — платёж и CRM

8. Оплатить тестовой картой на странице ЮKassa, не показывая реквизиты карты в записи.
9. Показать статус «Оплачен» в CRM, запись `Payment`, принятый webhook и отсутствие
   дубликатов заказа/платежа.
10. Показать созданный заказ, позиции, канал website, доставку и историю статусов.

### Часть C — Telegram и омниканальность

11. В Telegram выполнить `/start`, показать явное согласие и продолжение регистрации
    по кнопке телефона без повторного `/start`.
12. Добавить товар вручную из каталога, затем написать «Давайте оформим заказ» — AI
    должен увидеть ту же корзину и продолжить с текущего шага.
13. На запрос описания товара показать текст, полученный из CRM, а не из памяти модели.

### Часть D — email и dashboard

14. Отправить письмо с заказом, получить preview и ответить отдельным письмом
    «Согласен» или «Оформляйте».
15. Проверить создание одного заказа и наличие ссылки ЮKassa в SMTP-ответе.
16. Открыть `/manager/dashboard/`: показать фильтр периода, количество/выручку,
    средний чек, каналы, оплаты, доставку, новых клиентов, топ товаров и динамику.

## 4. Перечень рекомендуемых скриншотов

Скриншоты делает автор диплома вручную после появления тестовых данных:

1. главная страница и каталог;
2. интерактивное согласие website AI;
3. диалог: поиск по смыслу → общая корзина → расчёт доставки → preview;
4. номер заказа и ссылка тестовой ЮKassa;
5. оплаченный заказ и payment audit в Django Admin;
6. Telegram: согласие, общая корзина и продолжение через AI;
7. email preview и ответ со ссылкой оплаты;
8. dashboard с графиком и топом товаров;
9. успешный GitHub Actions workflow;
10. схема из `docs/DATA_FLOW.md`.

До сохранения изображения скрыть телефон, email, полный адрес, Telegram/VK ID,
ключи, токены, cookies, реквизиты тестовой карты и содержимое `.env`.

## 5. Ограничения демонстрационного контура

- Яндекс Доставка и ЮKassa остаются в test/sandbox.
- Тестовая касса ЮKassa имитирует фискализацию и не отправляет реальный чек через ОФД.
- Реквизиты оператора и налоговые параметры production-кассы не входят в обязательный
  scope учебного MVP.
- MAX, production-доставка, реальные платежи и коммерческий SLA не демонстрируются.

## 6. Критерий успешной демонстрации

Цепочка считается подтверждённой, если фактический каталог приводит к общей корзине,
backend рассчитывает доставку и preview, одно явное подтверждение создаёт ровно один
CRM-заказ и один платёж, тестовый webhook меняет его статус, а dashboard отражает
операцию. Ошибка внешнего сервиса должна быть показана честно и не приводить к
выдуманной цене либо созданию неполного заказа.
